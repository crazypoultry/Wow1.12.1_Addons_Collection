<Bindings>
	<Binding name="SSGROUP" runOnUp="true" header="SSPVP">
		if( keystate == "down" ) then
			SSPVP_Enable_GroupQueue();
		elseif( keystate == "up" ) then
			SSPVP_Disable_GroupQueue();
		end
	</Binding>

	<Binding name="SSSOLO" runOnUp="true">
		if( keystate == "down" ) then
			SSPVP_Enable_SoloQueue();
		elseif( keystate == "up" ) then
			SSPVP_Disable_SoloQueue();
		end
	</Binding>

	<Binding name="TEFC">
		SSPVP_TargetEnemyFlag();
	</Binding>

	<Binding name="TFFC">
		SSPVP_TargetFriendlyFlag();
	</Binding>
</Bindings>
