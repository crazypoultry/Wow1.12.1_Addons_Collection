<Bindings>
	<Binding name="TOGGLE" header="LVBM" runOnUp="true">
		if (keystate == "down") then
			if LVBMBossModFrame:IsShown() then
				HideUIPanel(LVBMBossModFrame);
			else
				ShowUIPanel(LVBMBossModFrame);
			end
		end
	</Binding>
</Bindings>


<!-- RangeCheck etc. -->

