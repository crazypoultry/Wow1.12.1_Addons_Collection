<Bindings>
<!--	<Binding name="RECIPEBOOK_SKILL" header="RECIPEBOOK">
		if ( keystate == "down" ) then
			RecipeBook_Print("Coming Soon!");
		end
	</Binding>
-->
	<Binding name="RECIPEBOOK_CONFIG" header="RECIPEBOOK">
		if ( keystate == "down" ) then
			if(RecipeBook_OptionsFrame:IsVisible()) then
				RecipeBook_OptionsFrame:Hide();
			else
				RecipeBook_OptionsFrame:Show();
			end
		end
	</Binding>
		<Binding name="RECIPEBOOK_SEARCH" header="RECIPEBOOK">
		if ( keystate == "down" ) then
			RecipeBook_SearchFrame:Show();
		end
	</Binding>
</Bindings>
