<Bindings>
	<Binding name="SPROCKET_KEYMENU1" header="SPROCKET" runOnUp="true">
		if ( keystate == "down" ) then
			Sprocket:BeginKeyMarking( 1, IsAltKeyDown() or false, IsControlKeyDown() or false, IsShiftKeyDown() or false );
		else
			Sprocket:EndKeyMarking( 1 );
		end
	</Binding>
	<Binding name="SPROCKET_KEYMENU2" header="SPROCKET" runOnUp="true">
		if ( keystate == "down" ) then
			Sprocket:BeginKeyMarking( 2, IsAltKeyDown() or false, IsControlKeyDown() or false, IsShiftKeyDown() or false );
		else
			Sprocket:EndKeyMarking( 2 );
		end
	</Binding>
	<Binding name="SPROCKET_KEYMENU3" header="SPROCKET" runOnUp="true">
		if ( keystate == "down" ) then
			Sprocket:BeginKeyMarking( 3, IsAltKeyDown() or false, IsControlKeyDown() or false, IsShiftKeyDown() or false );
		else
			Sprocket:EndKeyMarking( 3 );
		end
	</Binding>
	<Binding name="SPROCKET_KEYMENU4" header="SPROCKET" runOnUp="true">
		if ( keystate == "down" ) then
			Sprocket:BeginKeyMarking( 4, IsAltKeyDown() or false, IsControlKeyDown() or false, IsShiftKeyDown() or false );
		else
			Sprocket:EndKeyMarking( 4 );
		end
	</Binding>
	<Binding name="SPROCKET_KEYMENU5" header="SPROCKET" runOnUp="true">
		if ( keystate == "down" ) then
			Sprocket:BeginKeyMarking( 5, IsAltKeyDown() or false, IsControlKeyDown() or false, IsShiftKeyDown() or false );
		else
			Sprocket:EndKeyMarking( 5 );
		end
	</Binding>
	<Binding name="SPROCKET_KEYMENU6" header="SPROCKET" runOnUp="true">
		if ( keystate == "down" ) then
			Sprocket:BeginKeyMarking( 6, IsAltKeyDown() or false, IsControlKeyDown() or false, IsShiftKeyDown() or false );
		else
			Sprocket:EndKeyMarking( 6 );
		end
	</Binding>
	<Binding name="SPROCKET_KEYMENU7" header="SPROCKET" runOnUp="true">
		if ( keystate == "down" ) then
			Sprocket:BeginKeyMarking( 7, IsAltKeyDown() or false, IsControlKeyDown() or false, IsShiftKeyDown() or false );
		else
			Sprocket:EndKeyMarking( 7 );
		end
	</Binding>
	<Binding name="SPROCKET_KEYMENU8" header="SPROCKET" runOnUp="true">
		if ( keystate == "down" ) then
			Sprocket:BeginKeyMarking( 8, IsAltKeyDown() or false, IsControlKeyDown() or false, IsShiftKeyDown() or false );
		else
			Sprocket:EndKeyMarking( 8 );
		end
	</Binding>
</Bindings>