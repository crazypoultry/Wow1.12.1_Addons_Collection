<Bindings>
	<Binding name="LUDWIG_SHOW" header="LUDWIG">
		if keystate == "down" then
			if LudwigUIParent:IsShown() then
				LudwigUIParent:Hide()
			else
				LudwigUIParent:Show()
			end
		end
	</Binding>
</Bindings>