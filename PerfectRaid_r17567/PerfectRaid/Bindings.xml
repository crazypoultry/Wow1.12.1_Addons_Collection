<Bindings>
	<Binding name="PRTOGGLEHIDE">
		if PerfectRaid.master then
			if PerfectRaid.master:IsVisible() then
				PerfectRaid.master:Hide()
			else
				PerfectRaid.master:Show()
			end
		end
	</Binding>
</Bindings>
