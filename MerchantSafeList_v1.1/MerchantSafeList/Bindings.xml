<Bindings>
	<Binding name="MERCHANTSAFELIST_OPTIONS" runOnUp="true" header="MERCHANTSAFELIST">
		if ( keystate == "down" ) then
            if not getglobal("MerchantSafeList_Options"):IsVisible() then
                getglobal("MerchantSafeList_Options"):Show();
            else
                getglobal("MerchantSafeList_Options"):Hide();
            end
		end
	</Binding>
</Bindings>
