<Bindings>
	<Binding name='Mini-Map Zoom' description='Hold Key to expand the Minimap' runOnUp='true' header='MAPZOOM'>
		if ( keystate == "down" ) then
			ToggleMapZoom(1);
		else
			ToggleMapZoom(0);
		end
	</Binding>

	<Binding name='Static Toggle' description='Use this to statically toggle the MiniMap'>
		MapZoom_SlashHandler("toggle");
	</Binding>
</Bindings>